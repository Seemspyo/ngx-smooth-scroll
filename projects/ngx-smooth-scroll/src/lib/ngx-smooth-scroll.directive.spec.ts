import { NgxSmoothScrollDirective } from './ngx-smooth-scroll.directive';

describe('NgxSmoothScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxSmoothScrollDirective();
    expect(directive).toBeTruthy();
  });
});
